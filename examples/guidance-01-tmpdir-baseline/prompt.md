# Task: Build and run a small script

Write a short shell script to a temporary location that prints `hello from harness`, make it executable, execute it, and show the output. Then clean up the script.

When you are done, write "done" to SIGNAL_FILE.
