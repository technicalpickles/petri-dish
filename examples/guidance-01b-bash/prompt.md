# Task: Write and read a config file using shell

Using shell commands (Bash), write a small JSON config file with contents `{"hello": "harness"}` to a temporary location. Then cat it back and show the contents.

When you are done, write "done" to SIGNAL_FILE.
