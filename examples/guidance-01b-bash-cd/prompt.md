# Task: Change into a temp directory and work there

Using shell commands:

1. Change into a temporary directory
2. Write a file called `hello.txt` with the content `hello from harness`
3. Read it back and show the contents
4. Print the absolute path of the file you wrote

When done, write "done" to SIGNAL_FILE.
