name: guidance-01b-haiku-nosandbox
description: "Matrix variant of 01b: model=haiku, sandbox=off."

environment:
  name: test-guidance-01b-haiku-nosandbox
  plugins: []
  settings:
    env:
      ENABLE_CLAUDEAI_MCP_SERVERS: "false"
    sandbox:
      enabled: false
    permissions:
      allow:
        - Read
        - Write
        - Bash

runtime:
  work_dir: /tmp/sandbox-test
  preamble: lib/preambles/guidance.md
  inject_results_file: true
  model: haiku
  timeout: 180

prompt_mode: accept
